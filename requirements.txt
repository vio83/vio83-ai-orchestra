# VIO 83 AI ORCHESTRA - Dipendenze Python
# Installare con: pip install -r requirements.txt

# === Core Server ===
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# === HTTP Client (per chiamate Ollama/API) ===
httpx>=0.27.0

# === Knowledge Base — Document Ingestion ===
# PDF extraction (installa almeno uno):
# PyMuPDF>=1.24.0            # pip install PyMuPDF — il più veloce e preciso
# pdfplumber>=0.11.0         # pip install pdfplumber — buono per tabelle
pypdf>=4.0.0                 # Fallback leggero per PDF

# DOCX extraction:
python-docx>=1.1.0           # pip install python-docx

# EPUB extraction (opzionale):
# EbookLib>=0.18              # pip install EbookLib

# === Knowledge Base — Embedding e Vector Search ===
# sentence-transformers>=3.0.0  # Embedding locali (opzionale, ~500MB modello)
# chromadb>=0.5.0              # Vector DB (richiede Python <3.14)

# === Opzionali ===
# litellm>=1.50.0        # Multi-provider (richiede Python <3.14)
# aiohttp>=3.10.0        # HTTP alternativo
